import '../imports/startup/server'

Meteor.startup(() => {
    // code to run on server at startup
    // process.env.MAIL_URL =
    //     'smtp://SMTP_Injection:sparkpostAddress@smtp.sparkpostmail.com:2525'
    // Accounts.emailTemplates.from = 'foo@abar.com'
})
